对，你这个判断是对的。如果目标版本确定为 Zabbix 8.0，那么 Agent vs Agent 2 的判断应该以 Zabbix 8.0 devel 官方文档为准，而不是拿 6.x、7.x 时代的文章直接套用。

我刚刚查了 Zabbix 官方的 8.0 devel 文档、8.0 Agent/Agent2 对比页、8.0 Requirements 和 Roadmap。目前官方 Roadmap 显示 Zabbix 8.0 LTS 计划于 2026 年 9 月发布，所以现在仍应把 8.0 的资料理解为开发版资料，而不是已经正式 GA 的最终版本。

先给你结论

如果你的新系统准备直接做成 Zabbix 8.0：

> 我会优先选择 Agent 2。



而且这个推荐比 Zabbix 6 → 7 的时候更加明确。

原因不是简单的“Agent 2 更新”，而是从 Zabbix 8.0 官方文档来看：

Agent 仍然被正式支持；

Agent 2 也仍然被正式支持；

Agent 2 已经明确被定位成 Agent 的新一代替代方案；

Agent 2 保留 Agent 的既有功能；

Agent 2 在并发、插件、持久化、数据库/中间件监控等方面明显更强；

Zabbix 8.0 仍然没有宣布 Agent 1 被废弃，所以不是“Agent 不能用了”；

如果没有特殊兼容性要求，8.0 新部署选择 Agent 2 更合理。



---

1. Zabbix 8.0 官方现在到底怎么定位 Agent 和 Agent 2？

8.0 官方介绍写得非常直接：

> Agent：轻量、支持很多平台、C 编写
Agent 2：更加灵活、容易通过插件扩展、Go 编写



而 Agent 2 官方说明则明确写着：

> Agent 2 是新一代 Zabbix Agent，并且是 Agent 的替代方案，同时支持之前的功能。



所以这里有一个非常重要的理解：

不是：

Agent 1 → 已废弃
Agent 2 → 唯一选择

而是：

Zabbix 8.0
                     │
          ┌──────────┴──────────┐
          │                     │
      Zabbix Agent          Zabbix Agent 2
          │                     │
        C语言                  Go + C
          │                     │
      传统架构              Plugin架构
          │                     │
      轻量/兼容性          扩展性/并发/功能

官方 8.0 仍然同时维护两者。


---

2. Zabbix 8.0 最大的区别：架构

这是我认为你最应该关注的。

Agent

Zabbix Agent：

Zabbix Agent
    │
    ├── C
    ├── native system calls
    ├── built-in metrics
    ├── UserParameter
    └── C loadable module

官方明确说明 Agent 使用 C 编写，并且通过 native system calls 获取系统指标。


---

Agent 2

Agent 2：

Agent 2
                    │
          ┌─────────┴─────────┐
          │                   │
       Core/Go             Plugins
          │                   │
          │          ┌────────┼────────┐
          │          │        │        │
          │        Docker    DB     systemd
          │
          └──── UserParameter

官方 8.0 文档明确：

Agent 2 使用 Go；

部分代码复用 Agent 的 C 代码；

所有 Agent 2 的 metrics 都由 plugins 收集；

有 built-in plugins；

有 loadable plugins；

可以自己开发 plugin；

loadable plugin 可以在运行时加入，而不需要重新编译整个 Agent 2。


这实际上是 Agent 2 最大的战略优势。


---

3. 你最应该关注的：并发能力

这个在 Zabbix 8.0 官方对比表里仍然非常明确。

Agent

对于一个 server：

Active check 1
      ↓
Active check 2
      ↓
Active check 3
      ↓
Active check 4

官方描述：

> Active checks for single server are executed sequentially.



也就是单个 server 的 active checks 是顺序执行的。


---

Agent 2

Agent 2：

Plugin A ──────┐
               ├── concurrent
Plugin B ──────┤
               │
Plugin C ──────┘

不同 plugin 可以并发执行。

同一个 plugin 内也可以存在并发检查，不过受到 plugin capacity 限制。

所以如果你的机器上：

CPU
Memory
Disk
Network
Log
PowerShell
SQL
Docker
Custom script

同时大量监控，那么 Agent 2 的架构明显更加适合。


---

4. TCP connection

这也是 Agent 2 官方设计目标之一。

Zabbix 8.0 官方 Agent 2 页面明确把：

> Reduce the number of TCP connections



列为 Agent 2 的设计目标。

所以如果你监控：

大量 Item
大量 active check
大量 plugin
大量 host

Agent 2 的整体架构更适合这种场景。

不过这里我不建议简单理解成：

> Agent 2 一定比 Agent 1 快。



这个结论太绝对。

正确说法应该是：

> Agent 2 的架构针对连接复用、并发检查和插件化进行了优化，因此在复杂/高并发监控场景下具有更好的架构优势。



实际 CPU、Memory、响应时间还是取决于具体 workload。


---

5. Zabbix 8.0 的功能覆盖

这一点也非常关键。

官方 8.0 文档明确说：

> All item keys supported by Zabbix agent are also supported by Zabbix agent 2.



也就是说：

Agent 支持的 Item
        ↓
    Agent 2 也支持

同时 Agent 2 还有额外 Item。

例如：

Docker
PostgreSQL
Redis
MySQL
Memcached
systemd
Ceph
SMART
MongoDB
MSSQL
Oracle
NVIDIA GPU
...

8.0 官方文档已经把这些 Agent 2 plugin 单独列出来。

所以如果你以后要扩展：

Docker
DB
Redis
PostgreSQL
systemd
GPU
MongoDB
MSSQL

Agent 2 的优势会越来越明显。


---

6. 但是 Agent 1 有一个非常重要的优势：平台

这个对于你的实际部署环境非常重要。

Zabbix 8.0 官方对比表：

平台	Agent	Agent 2

Linux	✅	✅
Windows	✅	✅
AIX	✅	❌
FreeBSD	✅	❌
NetBSD	✅	❌
OpenBSD	✅	❌
HP-UX	✅	❌
Solaris	✅	❌
macOS	✅	❌


官方 8.0 comparison 明确列出的 Agent 2 支持平台主要是 Linux 和 Windows；Agent 则覆盖大量传统 Unix 平台。

所以：

如果是

Linux
Windows

→ Agent 2 优先

如果是

AIX
HP-UX
Solaris
FreeBSD
OpenBSD
NetBSD
macOS

→ Agent 1 更现实

这其实是 Agent 1 目前最重要的存在价值之一。


---

7. 如果你现在说的是 Windows

你之前提到你的 Agent 是 Windows + Agent 1。

如果你的新 Zabbix 是 8.0，那么我会更加倾向：

Windows
   ↓
Zabbix Agent 2

因为 Windows 是 Agent 2 的正式支持平台，而且 Agent 2 可以：

作为 Windows Service运行；

支持多个 Agent 2 instance；

支持 plugins；

支持 Agent 1 的 item keys；

支持 UserParameter；

支持更多原生监控能力。


所以你之前那个：

Windows
    ↓
Agent 1
    ↓
my.agent.script[ABC123]

迁移到 Zabbix 8：

Windows
    ↓
Agent 2
    ↓
my.agent.script[ABC123]

整体思路是可以继续使用的。

Agent 2 官方也明确支持 UserParameter。


---

8. Persistent Buffer：Agent 2 又多了一个比较重要的能力

Agent 1：

Agent
  ↓
Server unavailable
  ↓
data lost

Agent 2：

Agent 2
   ↓
Server unavailable
   ↓
Persistent Buffer
   ↓
local storage
   ↓
Server recovery
   ↓
send data

官方 8.0 comparison：

功能	Agent	Agent 2

Persistent storage	❌	✅


Agent 2 还有：

EnablePersistentBuffer
PersistentBufferFile
PersistentBufferPeriod

等配置。

如果你的网络环境存在：

Windows Agent
      ↓
网络偶尔断
      ↓
Zabbix Server

那么这个功能就比较有价值。


---

9. Agent 2 的 Plugin 是非常大的区别

Agent 1：

C loadable module

如果你想自己扩展 Agent：

C
↓
compile
↓
module
↓
Agent

Agent 2：

Go Plugin
     ↓
Loadable Plugin
     ↓
Agent 2

而且官方明确说：

> Loadable plugins can be added at runtime without recompiling Zabbix Agent 2.



这对于以后做二次开发非常重要。


---

10. 这点对你现在的项目尤其重要

你现在实际上有两个不同层面的“二次开发”：

第一种

你之前的 Zabbix 6：

Zabbix Source
      ↓
修改 C / PHP
      ↓
重新编译
      ↓
自己的 Zabbix

这种属于：

> 修改 Zabbix 本体。




---

第二种

如果未来迁移到 Zabbix 8：

Zabbix 8
   │
   ├── 官方代码
   │
   └── Agent 2
          │
          └── Custom Plugin

你的自定义功能可以尽量往：

Agent 2 Plugin

方向走。

这样可以减少：

修改 Zabbix Core
↓
升级困难
↓
重新 merge
↓
重新解决 conflict

这对于你现在正在做的 Zabbix 6 二次开发 → 新版 Zabbix 迁移，其实是非常重要的。


---

11. 但是有一个地方 Agent 1 反而更方便

就是配置和运行方式。

Agent 1：

zabbix_agentd

自身就支持 daemonization。

Agent 2 在 Linux 上：

zabbix_agent2
       ↓
foreground process
       ↓
systemd
       ↓
background service

官方 8.0 文档明确说明 Agent 2 在 Unix-like 系统上没有内置 daemonization，而是依赖 systemd 等外部 service manager。

所以如果你的 Linux 运维体系非常传统：

/etc/init.d/
/var/run/
/daemon

那么 Agent 1 的行为可能更符合老系统习惯。

不过现代 Linux：

systemd

基本不是问题。


---

12. Log monitoring 这里要特别注意

官方 8.0 comparison 还有一个容易被忽略的差异：

功能	Agent	Agent 2

Persistent files for log*[]	✅ Unix	❌
Log data upload while gathering	✅	❌


Agent 2 的 log gathering 在 buffer 满了以后会停止，因此官方特别要求：

BufferSize >= MaxLinesPerSecond × 2



这个对于你之前一直关注的 Zabbix slow query log / 日志监控 是值得注意的。

如果你以后大量使用：

log[]
logrt[]
log.count[]

建议迁移时专门测试 Agent 2 的日志行为，而不要因为“Item key 一样”就认为完全没有差异。


---

13. 还有一个容易被忽略的区别：User / 权限

Agent 1：

User=zabbix

并且 Agent 自己可以处理一些用户切换行为。

Agent 2：

systemd
   ↓
User=
   ↓
zabbix_agent2

官方 8.0 comparison 明确：

Changes user at runtime

Agent     yes
Agent 2   no, controlled by systemd



所以如果你有：

UserParameter
system.run
PowerShell
custom script

迁移的时候一定要检查：

执行用户
文件权限
目录权限
环境变量
PATH
Windows Service account

而不是只检查 Item key。


---

14. Agent 2 并不是完全没有代价

所以我不建议简单地说：

> Zabbix 8 = Agent 2 全面取代 Agent。



目前官方并没有这么说。

实际上 Agent 1 仍然有几个很现实的优势：

① 平台覆盖更广

特别是：

AIX
HP-UX
Solaris
BSD
macOS

② 更传统、更简单

如果你只是：

CPU
Memory
Disk
Network
Process
File
Log
UserParameter

Agent 1 已经非常成熟。

③ 你的老 Agent 1 环境迁移成本可能更低

尤其你现在已经存在：

大量 Agent 1
+
大量 UserParameter
+
大量 custom script

如果只是为了升级 Server 到 Zabbix 8，并没有必要为了“新”而一次性把所有 Agent 都换掉。


---

15. Zabbix 8 官方甚至明确允许 Agent 继续使用

这个非常重要。

官方 8.0 compatibility：

Agent

Zabbix Agent >= 1.4
and
Agent <= 8.0

Agent 2

Agent 2 >= 4.4
and
Agent 2 <= 8.0

也就是说 Zabbix 8.0：

Server 8
    │
    ├── Agent 1
    │
    └── Agent 2

两者都可以。

所以你完全可以：

Zabbix Server 8
       │
       ├── Linux Server A → Agent 2
       ├── Windows Server B → Agent 2
       ├── Windows Server C → Agent 1
       ├── AIX Server D → Agent 1
       └── Solaris E → Agent 1

这其实是非常合理的架构。


---

16. 如果你的目标就是 Zabbix 8，我给你的选择建议

我会这样定：

场景	推荐

新部署 Linux	Agent 2
新部署 Windows	Agent 2
Docker	Agent 2
PostgreSQL	Agent 2
Redis	Agent 2
MySQL	Agent 2
systemd	Agent 2
需要自定义 Plugin	Agent 2
大量并发 Active Check	Agent 2
需要 Persistent Buffer	Agent 2
AIX	Agent 1
HP-UX	Agent 1
Solaris	Agent 1
BSD	Agent 1
现有 Agent 1，运行稳定	没必要强制迁移
大量旧 UserParameter	先测试，再决定
大量 log[] / logrt[]	迁移前重点测试



---

17. 对你现在这个 Zabbix 6 → Zabbix 8 项目，我反而建议这样做

你现在不是单纯部署一个 Zabbix。

你的实际情况是：

旧 Zabbix 6.0
    │
    ├── 官方 Zabbix
    │
    └── 二次开发
          │
          ├── 自定义功能
          ├── 自定义脚本
          └── 可能修改过源码

准备变成：

Zabbix 8
    │
    ├── 官方 Zabbix 8
    │
    ├── 尽可能不修改 Core
    │
    ├── Agent 2
    │
    └── Plugin / Webhook / API / External script

我认为这是比：

Zabbix 6 + Agent 1
        ↓
Zabbix 8 + Agent 1

更加值得考虑的路线。

尤其你之前一直强调：

> 不要修改 Zabbix 官方 C 源码。



那么 Agent 2 的插件架构实际上非常符合你的这个目标。


---

18. 但有一点我建议你现在不要急着定死

因为目前官方 Roadmap 给出的 Zabbix 8.0 LTS 是：

2026 年 9 月计划发布。

而现在官方文档仍然标注：

Version: 8.0 (devel)

所以现在看到的 8.0 文档应该理解成：

> 当前 Zabbix 8 开发版官方设计/预期行为



而不是：

> 最终 8.0.0 GA 一定 100% 如此。





因此如果你现在正在做迁移设计，我建议把：

> Zabbix 8 官方 devel 文档



作为设计依据；等 8.0 LTS 正式发布后，再针对 8.0.0 最终版的 Agent/Agent2 comparison、Requirements、release notes 做一次最终确认。


---

最后一句话

如果把问题限定为：

> “我要部署一个全新的 Zabbix 8，Windows/Linux 都是现代操作系统，没有 AIX/HP-UX/Solaris 等特殊平台，而且希望以后尽量少改 Zabbix Core。”



那么我的选择会非常明确：

Server：Zabbix 8
→ Linux/Windows：优先 Agent 2
→ 特殊 Unix 平台：Agent 1
→ 已经稳定运行的旧 Agent 1：不强制迁移。

而且这不是因为“Agent 2 比 Agent 1 新”这么简单，而是因为 Zabbix 8 官方已经把 Agent 2 明确定位成新一代、可扩展、插件化、并发能力更强的 Agent 替代方案。

如果你愿意，我下一步可以进一步针对你的实际情况，。这个对你现在的项目会比单纯比较 Agent/Agent2 更有用。专门整理一份 **「Zabbix 6 Agent 1 → Zabbix 8 Agent 2 迁移时，到底哪些配置、UserParameter、脚本、Item Key、Windows Service 会受到影响」** 的迁移检查表
